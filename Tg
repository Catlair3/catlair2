import asyncio
import logging
from aiogram import Bot, Dispatcher, types
from aiogram.filters.command import Command

# Включаем логирование, чтобы не пропустить важные сообщения
logging.basicConfig(level=logging.INFO)
# Объект бота
bot = Bot(token="12345678:AaBbCcDdEeFfGgHh")
# Диспетчер
dp = Dispatcher()

# Хэндлер на команду /start
@dp.message(Command("start"))
async def cmd_start(message: types.Message):
    await message.answer()
@dp.message(Command("shutery"))
async def cmd_start(message: types.Message):
  photo=FSInputFile()
 await message.answer.photo(photo=..., caption=...)

@dp.message(Command("race"))
async def cmd_start(message: types.Message):
  photo=FSInputFile()
 await message.answer.photo(photo=..., caption=...)

@dp.message(Command("CRMP"))
async def cmd_start(message: types.Message):
  photo=FSInputFile()
 await message.answer.photo(photo=..., caption=...)

@dp.message(Command("simulator of life"))
async def cmd_start(message: types.Message):
  photo=FSInputFile()
 await message.answer.photo(photo=..., caption=...)

@dp.message(Command("arcade"))
async def cmd_start(message: types.Message):
  photo=FSInputFile()
 await message.answer.photo(photo=..., caption=...)

@dp.message(Command("action adventure"))
async def cmd_start(message: types.Message):
  photo=FSInputFile()
 await message.answer.photo(photo=..., caption=...)

@dp.message(Command(""))
async def cmd_start(message: types.Message):
  photo=FSInputFile()
 await message.answer.photo(photo=..., caption=...)
async def main():
    await dp.start_polling(bot)

if __name__ == "__main__":
    asyncio.run(main())
